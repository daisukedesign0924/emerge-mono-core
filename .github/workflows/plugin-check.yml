name: Plugin Check

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  plugin-check:
    name: WordPress Plugin Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Prepare distribution candidate
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p build/emerge-mono-core
          rsync -a \
            --exclude='.git' \
            --exclude='.github' \
            --exclude='build' \
            --exclude='dist' \
            --exclude='tests' \
            ./ build/emerge-mono-core/
      - name: Run official WordPress Plugin Check
        uses: wordpress/plugin-check-action@v1
        with:
          build-dir: './build/emerge-mono-core'
